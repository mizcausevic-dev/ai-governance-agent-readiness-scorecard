This repo started as an offline analyzer and CLI for executive AI-governance posture.

The public control surface layers on top of that analyzer so the same synthetic packets can be read as:

- executive score
- agent-readiness lanes
- risk benchmarks
- board-brief packets

That keeps the repo grounded in a reusable product primitive rather than static marketing copy.
